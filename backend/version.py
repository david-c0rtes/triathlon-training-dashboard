"""The single source of truth for TriFlow's version — bump this per release,
tag the matching git tag (vX.Y.Z), and attach the installer to that GitHub
Release so services/update_check.py can find it."""
VERSION = "0.1.1"
